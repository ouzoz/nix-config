# System Configuration

- asd
    1. asd
    2. dsa

## Commands

### Nix

#### Templates

```bash
# enable dev shell
nix-shell

# allow .envrc
direnv allow

# initialize a template
nix flake init -t /etc/nixos#empty
```
