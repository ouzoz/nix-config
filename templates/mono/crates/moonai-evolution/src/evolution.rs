use crate::genome::Genome;

pub struct EvolutionManager;

impl EvolutionManager {
    pub fn seed_initial_population() -> Vec<Genome> {
        Vec::new()
    }

    pub fn reproduce_population() {}
}
